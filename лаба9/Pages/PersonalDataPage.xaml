<Page x:Class="MultiPageForm.Pages.PersonalDataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Личные данные">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Личные данные" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Имя:" Margin="0,0,0,5"/>
            <TextBox x:Name="FirstNameTextBox"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Фамилия:" Margin="0,0,0,5"/>
            <TextBox x:Name="LastNameTextBox"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Дата рождения:" Margin="0,0,0,5"/>
            <DatePicker x:Name="BirthDatePicker"/>
        </StackPanel>

        <Button Grid.Row="4" Content="Далее" HorizontalAlignment="Right" 
                Padding="20,5" Click="NextButton_Click"/>
    </Grid>
</Page>